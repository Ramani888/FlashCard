import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const ImageModalContent = ({closeModal}) => {
  return (
    <View>
      <Text>ImageModalContent</Text>
    </View>
  )
}

export default ImageModalContent

const styles = StyleSheet.create({})